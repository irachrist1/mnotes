# ── AI Auth (pick one or more) ───────────────────────────────────────────────
# Option 1: Anthropic API key (API billing)
ANTHROPIC_API_KEY=

# Option 2: Claude subscription (local mode)
# Just run `claude` CLI and log in. No env var needed — auto-detected from ~/.claude/session-env/ (OAuth)
# or ~/.claude/credentials.json (legacy API key format).

# Option 3: Google AI Studio / Gemini API (ai.google.dev)
# Any one of these key names is accepted:
GOOGLE_AI_KEY=
# GOOGLE_API_KEY=
# GEMINI_API_KEY=

# ── Model overrides (optional) ────────────────────────────────────────────────
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
GOOGLE_MODEL=gemini-3-flash-preview

# ── Convex connection (for MCP servers to read/write user data) ───────────────
CONVEX_URL=https://your-project.convex.cloud
CONVEX_DEPLOY_KEY=

# ── Agent server settings ─────────────────────────────────────────────────────
PORT=3001
AGENT_SERVER_SECRET=your-random-secret-here

# ── Microsoft (for Outlook MCP) ───────────────────────────────────────────────
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=

# ── Next.js app URL (for CORS) ────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
