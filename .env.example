# Convex
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Clerk Authentication (optional, app works without it)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# Convex Dashboard: Set CLERK_JWT_ISSUER_DOMAIN to your Clerk issuer URL
# e.g., https://your-app.clerk.accounts.dev

# AI keys are stored in Convex userSettings (via Settings page).
# These NEXT_PUBLIC_ keys are deprecated and no longer used.
# NEXT_PUBLIC_OPENROUTER_API_KEY=
# NEXT_PUBLIC_GOOGLE_GEMINI_API_KEY=

# PostHog Analytics
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
# Server-side (set in Convex dashboard for LLM analytics):
# POSTHOG_API_KEY=
# POSTHOG_HOST=https://us.i.posthog.com

# Connectors (OAuth) - set these in Convex env (NOT in Next.js env).
# Used for Gmail + Google Calendar OAuth connect + token refresh.
#
# You must also register the redirect URI in Google Cloud Console:
#   https://<your-deployment>.convex.site/connectors/google/callback
#
# CONVEX_SITE_URL should be your Convex deployment site base URL:
#   https://<your-deployment>.convex.site
#
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# CONVEX_SITE_URL=

# GitHub OAuth (set in Convex env).
# Redirect URI:
#   https://<your-deployment>.convex.site/connectors/github/callback
#
# GITHUB_OAUTH_CLIENT_ID=
# GITHUB_OAUTH_CLIENT_SECRET=
